RewriteEngine On

# Let real files and directories be served directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all other requests to the React app entrypoint.
RewriteRule ^ index.html [L]
